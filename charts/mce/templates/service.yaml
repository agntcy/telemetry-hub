apiVersion: v1
kind: Service
metadata:
  name: {{ include "obs-mce.fullname" . }}
  labels:
    {{- include "obs-mce.labels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "obs-mce.selectorLabels" . | nindent 4 }}
